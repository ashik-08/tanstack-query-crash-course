{
  "name": "server",
  "version": "1.0.0",
  "description": "mock server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./database/data.json --host 0.0.0.0 --port 3000"
  },
  "author": "Ashikur Rahman",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
